package cs301.birthdaycake;

/* Models it the logic behind an app
 *  -Only deals with data
 *  -Include mostly instance variables and data handling methods
 */
public class CakeModel {
    public boolean candle_lit = true;
    public int candle_num = 2;
    public boolean frosting = true;
    public boolean hasCandles = true;
}
